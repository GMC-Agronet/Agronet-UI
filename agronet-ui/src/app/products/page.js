import ProductCard from '@/app/components/ProductCard';

export default function Page() {
  return (
    <div>
      {/* You can add page content here, or leave it empty if not used */}
    </div>
  );
}
